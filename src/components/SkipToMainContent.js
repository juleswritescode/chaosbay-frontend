import React from 'react';

const SkipToMainContent = () => {
    return (
        <a
            href="#navigation"
            className="access-button hidden lg:inline-block z-10"
        >
            Skip To Navigation
        </a>
    );
};

export default SkipToMainContent;

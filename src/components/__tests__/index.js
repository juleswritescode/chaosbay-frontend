describe('Home Page', () => {
    it('should not show header, social icons and menu div on first render', () => {
        expect(true).toBeTruthy();
    });
    it('Should make the logo disappear/reappear based on mobile state', () => {
        expect(true).toBeTruthy();
    });
});

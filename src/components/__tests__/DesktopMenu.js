describe('DesktopMenu', () => {
    it('should trap focus whenever an item is opened/closed', () => {});
});

describe('MobileMenu', () => {
    it('should trap focus whenever a section is opened or closed', () => {});
});
